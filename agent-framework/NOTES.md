# Agent Framework - Async Agent with Custom Tools

## Overview
Uses `agent-framework` library (async) to create AI agent with custom Python function tools. Example: expense claim submission via email.

## Workflow
Load data → User prompt → Agent analyzes → Calls `send_email` tool → Returns confirmation
